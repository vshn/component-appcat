parameters:
  appcat:
    =_metadata:
      multi_tenant: true

    charts:
      crossplane:
        source: https://charts.crossplane.io/stable
        version: 1.20.1
      redis:
        source: oci://registry-1.docker.io/bitnamicharts/redis
        version: 23.2.1
      mariadb:
        source: oci://registry-1.docker.io/bitnamicharts/mariadb-galera
        version: 16.0.1
      minio:
        source: https://charts.min.io
        version: 5.4.0
      keycloak:
        source: https://codecentric.github.io/helm-charts
        version: 7.1.5
      nextcloud:
        source: https://nextcloud.github.io/helm/
        version: 8.6.0
      forgejo:
        source: oci://code.forgejo.org/forgejo-helm/forgejo
        version: 15.0.3
      cnpg:
        source: https://cloudnative-pg.io/charts/
        version: 0.26.1
        # Be aware that the cnpg post-process filter will append "-ubiX" to the operator image.
        # The UBI version might increase with newer versions, so check with skopeo first:
        # skopeo list-tags docker://ghcr.io/cloudnative-pg/cloudnative-pg
      cnpgCluster:
        # https://artifacthub.io/packages/helm/cloudnative-pg/cluster
        source: https://cloudnative-pg.io/charts/
        version: 0.3.1
        chartName: cluster
    images:
      provider-kubernetes:
        registry: ghcr.io
        repository: crossplane-contrib/provider-kubernetes
        tag: v1.1.0
      provider-helm:
        registry: ghcr.io
        repository: crossplane-contrib/provider-helm
        tag: v1.0.6
      provider-exoscale:
        registry: ghcr.io
        repository: vshn/provider-exoscale
        tag: v1.0.1
      provider-cloudscale:
        registry: ghcr.io
        repository: vshn/provider-cloudscale
        tag: v0.5.10
      provider-minio:
        registry: ghcr.io
        repository: vshn/provider-minio
        tag: v0.4.5
      provider-sql:
        registry: ghcr.io
        repository: vshn/provider-sql
        tag: v0.11.0-vshn
      sloth:
        registry: ghcr.io
        image: slok/sloth
        tag: v0.11.0
      statefulset-resize-controller:
        registry: ghcr.io
        repository: vshn/statefulset-resize-controller
        tag: v0.3.1
      appcat:
        registry: ghcr.io
        repository: vshn/appcat
        tag: v4.178.0
      functionAppcat:
        registry: ${appcat:images:appcat:registry}
        repository: ${appcat:images:appcat:repository}
        tag: ${appcat:images:appcat:tag}-func
      functionpnt:
        registry: xpkg.upbound.io
        repository: crossplane-contrib/function-patch-and-transform
        tag: v0.9.1
      reporting:
        registry: ghcr.io
        repository: appuio/appuio-reporting
        tag: v0.2.2
      collector:
        registry: ghcr.io
        repository: vshn/billing-collector-cloudservices
        tag: v3.6.0
      kubectl:
        registry: docker.io
        image: bitnamilegacy/kubectl
        tag: "1.25.15"
      proxysql:
        registry: docker.io
        image: proxysql/proxysql
        version: "3.0.3"
      collabora:
        registry: docker.io
        image: collabora/code
        tag: "latest"
      nextcloud:
        registry: docker.io
        image: library/nextcloud
        maintenanceURL: "https://hub.docker.com/v2/namespaces/library/repositories/nextcloud/tags?page_size=100"
      minio:
        maintenanceURL: "https://hub.docker.com/v2/repositories/minio/minio/tags/?page_size=100"
      busybox:
        registry: docker.io
        image: library/busybox
      forgejo:
        registry: code.forgejo.org
        maintenanceURL: "https://codeberg.org/v2/forgejo/forgejo/tags/list?page_size=100"
      redis:
        registry: ghcr.io
        repositoryPrefix: vshn
        maintenanceURL: "https://ghcr.io/v2/vshn/redis/tags/list?page_size=100"
      mariadb:
        registry: docker.io
        repositoryPrefix: bitnamilegacy
        maintenanceURL: "https://hub.docker.com/v2/repositories/bitnamilegacy/mariadb-galera/tags/?page_size=100"
      keycloak:
        registry: docker-registry.inventage.com:10121
        image: keycloak-competence-center/keycloak-managed
        maintenanceURL: "https://docker-registry.inventage.com:10121/v2/keycloak-competence-center/keycloak-managed/tags/list"
    =_crd_version: ${appcat:images:appcat:tag}

    pullPolicy: IfNotPresent
    namespace: syn-appcat
    namespaceLabels: {}
    namespaceAnnotations:
      openshift.io/node-selector: node-role.kubernetes.io/infra=

    appuioManaged: true
    quotasEnabled: false
    billingEnabled: false
    monitoringEnabled: true
    grpcEndpoint: host.docker.internal:9443
    proxyFunction: false

    bucketRegionOverride: ""

    cloudRegionMap:
      cloudscale:
        - lpg
        - rma
      exoscale:
        - ch-gva-2
        - ch-dk-2
        - de-fra-1
        - de-muc-1
        - at-vie-1
        - at-vie-2
        - bg-sof-1

    # Single or multiple cluster setup
    clusterManagementSystem:
      controlPlaneCluster: true
      serviceCluster: true
      controlPlaneKubeconfig: ""
      serviceClusterKubeconfigs: []

    # controls how comp revisions are handled
    deploymentManagementSystem:
      hotfix: ""
      enabled: true
      additionalFunctionBranches: []
      minimumRevisionAge: 168h # 7 days

    crossplane:
      namespace: syn-crossplane
      namespaceLabels: {}
      namespaceAnnotations:
        openshift.io/node-selector: node-role.kubernetes.io/infra=
      monitoring:
        enabled: false
        prometheus_rule_labels:
          prometheus: platform
        instance: null
      helmValues:
        customAnnotations:
          function-revision: ${appcat:images:appcat:tag}
        xfn:
          enabled: true
        args:
          - --enable-usages
          - --poll-interval=1h
          - --enable-realtime-compositions=false
          - --registry=xpkg.upbound.io
        image:
          pullPolicy: ${appcat:pullPolicy}
        alpha:
          oam:
            enabled: false
        resourcesCrossplane:
          limits:
            cpu: 1000m
          requests:
            cpu: 10m
        resourcesRBACManager:
          limits:
            cpu: 1000m
          requests:
            cpu: 10m
        metrics:
          enabled: ${appcat:crossplane:monitoring:enabled}
        webhooks:
          enabled: true
        rbacManager:
          managementPolicy: Basic

    cnpg:
      namespace: syn-cnpg-system
      namespaceLabels: {}
      namespaceAnnotations: ${appcat:namespaceAnnotations}
      monitoring:
        enabled: false
        prometheus_rule_labels:
          prometheus: platform
      helmValues:
        additionalEnv:
          - name: EXPIRING_CHECK_THRESHOLD
            value: "15"
        resources:
          limits:
            cpu: 1000m
          requests:
            cpu: 100m

    billing:
      namespace: ${appcat:namespace}
      ignoreNamespace: vshn-test
      tenantID: ${cluster:tenant}
      clusterID: ${cluster:name}
      cloudZone: ""
      instanceUOM: uom_uom_45_1e112771
      customResourceDeletionAfter: "180"
      enableMockOrgInfo: false
      salesOrder: ${facts:sales_order}
      vshn:
        enableCronjobs: false
        schedule: 30 * * * *
        # Deploy metering prometheus rules
        meteringRules: true
      cloud:
        appuioControlKubeConfig: "?{vaultkv:__shared__/__shared__/appcat/appuio-control-kubeconfig}"
        secrets:
          exoscale:
            credentials:
              stringData:
                EXOSCALE_API_KEY: "?{vaultkv:${cluster:tenant}/${cluster:name}/billing-collector-cloudservices/exoscale-key}"
                EXOSCALE_API_SECRET: "?{vaultkv:${cluster:tenant}/${cluster:name}/billing-collector-cloudservices/exoscale-secret}"
                KUBERNETES_SERVER_URL: "?{vaultkv:${cluster:tenant}/${cluster:name}/billing-collector-cloudservices/cluster-server}"
                KUBERNETES_SERVER_TOKEN: "?{vaultkv:${cluster:tenant}/${cluster:name}/billing-collector-cloudservices/cluster-token}"
          cloudscale:
            credentials:
              stringData:
                CLOUDSCALE_API_TOKEN: "?{vaultkv:${cluster:tenant}/${cluster:name}/billing-collector-cloudservices/cloudscale-token}"
                KUBERNETES_SERVER_URL: "?{vaultkv:${cluster:tenant}/${cluster:name}/billing-collector-cloudservices/cluster-server}"
                KUBERNETES_SERVER_TOKEN: "?{vaultkv:${cluster:tenant}/${cluster:name}/billing-collector-cloudservices/cluster-token}"
        # Unit of measure map "cloud service value": "odoo16 value"
        uom:
          GB: uom_uom_71_6f28fc21
          GBDay: uom_uom_78_b847edc1
          KReq: uom_uom_60_a83156c7
          InstanceHour: uom_uom_45_1e112771
        exoscale:
          enabled: false
          dbaas:
            enabled: false
            # in minutes
            collectIntervalMinutes: 15
          # Exoscale metrics are generally fast
          # and we are less likely to miss and dbaas instances
          # in hours
          objectStorage:
            enabled: false
            collectIntervalHours: 23
            # Run the tool only after 6 in the morning.+
            # in hours
            billingHour: 6

        cloudscale:
          enabled: false
          # cloudscale.ch queries are rather slow.
          # Also the metrics are fetched for the previous day, won't change often.
          # in hours
          collectIntervalHours: 23
          # Run the tool only after 6 in the morning.+
          # in hours
          billingHour: 6
          # How many days ago to get the metrics
          # The result is always 1 day of metrics
          days: 1
      monitoring:
        enabled: true
        alerts:
          AppCatReportingDatabaseBackfillingFailed:
            enabled: true
            rule:
              annotations:
                description: AppCat Reporting backfilling metrics into Odoo failed
                message: AppCat Reporting backfilling metrics into Odoo failed.
                runbook_url: https://hub.syn.tools/appcat/runbooks/vshn-billing.html#AppCatReportingDatabaseBackfillingFailed
                summary: AppCat Reporting backfilling metrics into Odoo failed.
              expr: |
                kube_job_failed{job="kube-state-metrics",namespace="${appcat:namespace}",job_name=~"appcat-.*"} > 0
              for: 10m
              labels:
                severity: warning
                syn_team: schedar
      network_policies:
        target_namespaces:
          vshn-appuio-mimir: true
      prometheus:
        url: http://vshn-appuio-mimir-query-frontend.vshn-appuio-mimir.svc:8080/prometheus
        cloud_org_id: appuio-cloud-metering-c-appuio-cloudscale-lpg-2|appuio-cloud-metering-c-appuio-exoscale-ch-gva-2-0
        managed_org_id: appuio-managed-openshift-billing
      odoo:
        url: "https://test.central.vshn.ch/api/v2/product_usage_report_POST"
        db: "?{vaultkv:${cluster:tenant}/${cluster:name}/billing-collector-cloudservices/odoo-db}"
        oauth:
          url: "https://test.central.vshn.ch/api/v2/authentication/oauth2/token"
          clientID: "?{vaultkv:${cluster:tenant}/${cluster:name}/billing-collector-cloudservices/odoo-oauth-client-id}"
          clientSecret: "?{vaultkv:${cluster:tenant}/${cluster:name}/billing-collector-cloudservices/odoo-oauth-client-secret}"
      controlAPI:
        url: "?{vaultkv:${cluster:tenant}/${cluster:name}/billing-collector-cloudservices/cloud-api-url}"
        token: "?{vaultkv:${cluster:tenant}/${cluster:name}/billing-collector-cloudservices/cloud-api-token}"

    defaultRestoreRoleRules:
      - apiGroups:
          - vshn.appcat.vshn.io
        resources:
          - "*"
        verbs:
          - get
      - apiGroups:
          - "k8up.io"
        resources:
          - snapshots
        verbs:
          - get
      - apiGroups:
          - ""
        resources:
          - secrets
        verbs:
          - get
          - list
          - create
          - delete
      - apiGroups:
          - apps
        resources:
          - statefulsets/scale
        verbs:
          - update
          - patch
      - apiGroups:
          - apps
        resources:
          - statefulsets
        verbs:
          - get
      - apiGroups:
          - batch
        resources:
          - jobs
        verbs:
          - get
      - apiGroups:
          - ""
        resources:
          - events
        verbs:
          - get
          - create
          - patch
    controller:
      enabled: false
      additionalUnmanagedProtection:
        - group: ""
          version: v1
          resource: persistentvolumeclaims
      namespace: ${appcat:namespace}
      namespaceLabels: ${appcat:namespaceLabels}
      namespaceAnnotations: ${appcat:namespaceAnnotations}
      quotasEnabled: ${appcat:quotasEnabled}
      billingEnabled: ${appcat:billingEnabled}
      monitoringEnabled: ${appcat:monitoringEnabled}
      extraArgs: []
      extraEnv: {}
      controlPlaneKubeconfig: ${appcat:clusterManagementSystem:controlPlaneKubeconfig}
      billing:
        odooBaseURL: ${appcat:billing:odoo:url}
        odooDb: ${appcat:billing:odoo:db}
        odooClientID: ${appcat:billing:odoo:oauth:clientID}
        odooClientSecret: ${appcat:billing:odoo:oauth:clientSecret}
        odooTokenURL: ${appcat:billing:odoo:oauth:url}
      monitoring:
        crossplane_label_mapping: ${appcat:monitoring:crossplane:label_mapping}
        crossplane_extra_resources: ${appcat:monitoring:crossplane:extra_resources}
      tls:
        certSecretName: webhook-cert
      resources:
        requests:
          cpu: 100m
          memory: 50Mi
        limits:
          cpu: 200m
          memory: 300Mi

    stackgres:
      namespace: syn-stackgres-operator
      operator:
        channel: stable
        installPlanApproval: Manual
        resources: {}

    stsResizer:
      resources:
        requests:
          cpu: 100m
          memory: 50Mi
        limits:
          cpu: 250m
          memory: 200Mi

    monitoring:
      crossplane:
        extra_resources: ""
        label_mapping: |
          {
            "crossplane.io/claim-name": "claim_name",
            "crossplane.io/claim-namespace": "claim_namespace",
            "crossplane.io/composite": "instance_name"
          }
    apiserver:
      enabled: true
      namespace: ${appcat:namespace}
      namespaceLabels: ${appcat:namespaceLabels}
      namespaceAnnotations: ${appcat:namespaceAnnotations}
      extraArgs: []
      extraEnv: {}
      apiservice:
        insecureSkipTLSVerify: false
      tls:
        certSecretName: appcat-apiserver-tls
      resources:
        requests:
          cpu: 100m
          memory: 50Mi
        limits:
          cpu: 200m
          memory: 200Mi

    slos:
      enabled: true
      alertsEnabled: true
      uptimeDefaults:
        objective: 99.9
        alerting:
          page_alert:
            # This should reduce non actionable alerts because of single instance restarts.
            # The page alert looks (ammong other things) at the burn rate over the last 5min.
            # If the alert is pending for more than 5m this indicates a real problem.
            for: 6m
          ticket_alert: {}
      namespace: syn-appcat-slos
      namespaceLabels: ${appcat:namespaceLabels}
      namespaceAnnotations: ${appcat:namespaceAnnotations}
      sla_reporter:
        enabled: false
        resources:
          requests:
            cpu: 10m
            memory: 200Mi
          limits:
            cpu: 100m
            memory: 300Mi
        # 09:00 on the first of the month
        schedule: "0 9 1 * *"
        bucket_region: lpg
        slo_mimir_svc: vshn-appuio-mimir-query-frontend
        slo_mimir_namespace: vshn-appuio-mimir
        mimir_organization: appuio-managed-openshift-metrics
      sli_exporter:
        enableMaintenceObserver: false
        resources:
          requests:
            cpu: 10m
            memory: 200Mi
          limits:
            cpu: 100m
            memory: 300Mi
        kustomize_input:
          namespace: ${appcat:slos:namespace}
        controlPlaneKubeconfig: ${appcat:clusterManagementSystem:controlPlaneKubeconfig}
      alerting:
        labels:
          syn: "true"
          syn_team: schedar
          syn_component: appcat
          slo: "true"
        page_labels:
          severity: "critical"
        ticket_labels:
          severity: "warning"
      vshn:
        PostgreSQL:
          uptime: ${appcat:slos:uptimeDefaults}
        Redis:
          uptime: ${appcat:slos:uptimeDefaults}
        Minio:
          uptime: ${appcat:slos:uptimeDefaults}
        MariaDB:
          uptime: ${appcat:slos:uptimeDefaults}
        Keycloak:
          uptime: ${appcat:slos:uptimeDefaults}
        Nextcloud:
          uptime: ${appcat:slos:uptimeDefaults}
        Forgejo:
          uptime: ${appcat:slos:uptimeDefaults}

    providers:
      cloudscale:
        enabled: false
        apiVersion: cloudscale.crossplane.io/v1
        connectionSecretNamespace: syn-provider-cloudscale-secrets
        namespace: ${appcat:crossplane:namespace}
        credentials:
          name: cloudscale-api-token
          namespace: ${appcat:crossplane:namespace}
          data:
            CLOUDSCALE_API_TOKEN: "?{vaultkv:${cluster:tenant}/${cluster:name}/appcat/objectstorage/provider-cloudscale/token}"
        spec:
          package: ${appcat:images:provider-cloudscale:registry}/${appcat:images:provider-cloudscale:repository}:${appcat:images:provider-cloudscale:tag}
        runtimeConfig:
          serviceAccountName: provider-cloudscale
        defaultProviderConfig:
          credentials:
            source: InjectedIdentity
            apiTokenSecretRef:
              name: ${appcat:providers:cloudscale:credentials:name}
              namespace: ${appcat:crossplane:namespace}
        additionalProviderConfigs: []

      exoscale:
        enabled: false
        apiVersion: exoscale.crossplane.io/v1
        connectionSecretNamespace: syn-provider-exoscale-secrets
        namespace: ${appcat:crossplane:namespace}
        credentials:
          name: exoscale-api-access
          namespace: ${appcat:crossplane:namespace}
          data:
            EXOSCALE_API_KEY: "?{vaultkv:${cluster:tenant}/${cluster:name}/appcat/provider-exoscale/access-key}"
            EXOSCALE_API_SECRET: "?{vaultkv:${cluster:tenant}/${cluster:name}/appcat/provider-exoscale/secret-key}"
        spec:
          package: ${appcat:images:provider-exoscale:registry}/${appcat:images:provider-exoscale:repository}:${appcat:images:provider-exoscale:tag}
        runtimeConfig:
          serviceAccountName: provider-exoscale
        defaultProviderConfig:
          credentials:
            source: InjectedIdentity
            apiSecretRef:
              name: ${appcat:providers:exoscale:credentials:name}
              namespace: ${appcat:crossplane:namespace}
        additionalProviderConfigs: []

      kubernetes:
        enabled: false
        apiVersion: kubernetes.crossplane.io/v1alpha1
        namespace: ${appcat:crossplane:namespace}
        spec:
          package: ${appcat:images:provider-kubernetes:registry}/${appcat:images:provider-kubernetes:repository}:${appcat:images:provider-kubernetes:tag}
        runtimeConfig:
          serviceAccountName: provider-kubernetes
        defaultProviderConfig: {} # having this parameter will still create a ProviderConfig object with default values
        additionalProviderConfigs: []
        additionalRuntimeConfig: {}

      helm:
        enabled: false
        apiVersion: helm.crossplane.io/v1beta1
        namespace: ${appcat:crossplane:namespace}
        spec:
          package: ${appcat:images:provider-helm:registry}/${appcat:images:provider-helm:repository}:${appcat:images:provider-helm:tag}
        runtimeConfig:
          serviceAccountName: provider-helm
        defaultProviderConfig: {} # having this parameter will still create a ProviderConfig object with default values
        additionalProviderConfigs: []

      minio:
        enabled: false
        apiVersion: minio.crossplane.io/v1
        namespace: ${appcat:crossplane:namespace}
        spec:
          package: ${appcat:images:provider-minio:registry}/${appcat:images:provider-minio:repository}:${appcat:images:provider-minio:tag}
        runtimeConfig:
          serviceAccountName: provider-minio
        providerConfigRefs: []
        additionalProviderConfigs: []

      sql:
        enabled: ${appcat:services:vshn:postgres:enabled}
        namespace: ${appcat:crossplane:namespace}
        spec:
          package: ${appcat:images:provider-sql:registry}/${appcat:images:provider-sql:repository}:${appcat:images:provider-sql:tag}
        runtimeConfig:
          serviceAccountName: provider-sql
        providerConfigRefs: []
        additionalProviderConfigs: []

    services:
      enableDiffRBAC: false
      # Used for deploying jobs
      controlNamespace: "syn-appcat-control"
      # Works only for VSHN services for now
      emailAlerting:
        enabled: true
        smtpHost: "smtp.eu.mailgun.org:465"
        smtpUsername: appcat@appuio.cloud
        smtpPassword: "?{vaultkv:__shared__/__shared__/mailgun/smtp_password}"
        smtpFromAddress: appcat@appuio.cloud
        secretNamespace: syn-appcat
        secretName: mailgun-smtp-credentials
      vshn:
        ocpDefaultAppsDomain: ""
        enabled: false
        isServiceClusterOpenshift: false
        externalDatabaseConnectionsEnabled: "false"
        e2eTests: false
        quotasEnabled: ${appcat:quotasEnabled}
        secretNamespace: ${appcat:crossplane:namespace}
        forgejo:
          enabled: true
          billing: true
          serviceName: VSHNForgejo
          compFunctionsOnly: true
          connectionSecretKeys:
            - FORGEJO_HOST
            - FORGEJO_PASSWORD
            - FORGEJO_URL
            - FORGEJO_USERNAME
            - ca.crt
          mode: standalone
          offered: true
          restoreSA: forgejoserviceaccount
          restoreRoleRules: ${appcat:defaultRestoreRoleRules}
          additionalInputs:
            busybox_image: ${appcat:images:busybox:registry}/${appcat:images:busybox:image}
            ingress_annotations: |
              cert-manager.io/cluster-issuer: letsencrypt-production
          openshiftTemplate:
            serviceName: forgejobyvshn
            description: "Forgejo is a self-hosted lightweight software forge."
            message: 'Your Forgejo by VSHN instance is being provisioned, please see \${SECRET_NAME} for access.'
            url: https://vs.hn/vshn-forgejo
            tags: "codehosting,forgejo"
            icon: "icon-forgejo"
            defaultVersion: "22"
          enableNetworkPolicy: false
          secretNamespace: ${appcat:services:vshn:secretNamespace}
          helmChartVersion: ${appcat:charts:forgejo:version}
          imageRegistry: "${appcat:images:forgejo:registry}"
          maintenanceURL: "${appcat:images:forgejo:maintenanceURL}"
          grpcEndpoint: ${appcat:grpcEndpoint}
          proxyFunction: ${appcat:proxyFunction}
          defaultPlan: small
          sla: 99.25
          plans:
            mini:
              size:
                enabled: true
                cpu: "500m"
                memory: "2Gi"
                disk: 10Gi
            small:
              size:
                enabled: true
                cpu: "1"
                memory: "4Gi"
                disk: 50Gi
            medium:
              size:
                enabled: true
                cpu: "2"
                memory: "8Gi"
                disk: 200Gi
            large:
              size:
                enabled: true
                cpu: "4"
                memory: "16Gi"
                disk: 500Gi
        postgres:
          enabled: true
          billing: true
          maintenanceURL: https://raw.githubusercontent.com/cloudnative-pg/postgres-containers/main/Debian/ClusterImageCatalog-bookworm.yaml
          additionalInputs:
            kubectl_image: ${appcat:images:kubectl:registry}/${appcat:images:kubectl:image}:${appcat:images:kubectl:tag}
            # Depending on the cluster, this needs to be set
            loadbalancerAnnotations: ""
            cnpgClusterChartSource: ${appcat:charts:cnpgCluster:source}
            cnpgClusterChartVersion: ${appcat:charts:cnpgCluster:version}
            cnpgClusterChartName: ${appcat:charts:cnpgCluster:chartName}
          grpcEndpoint: ${appcat:grpcEndpoint}
          proxyFunction: ${appcat:proxyFunction}
          enableNetworkPolicy: true
          secretNamespace: ${appcat:services:vshn:secretNamespace}
          sgNamespace: syn-stackgres-operator
          defaultPlan: standard-2
          sla: 99.25
          plans:
            standard-2:
              size:
                enabled: true
                cpu: "400m"
                memory: "1936Mi"
                disk: 20Gi
            standard-4:
              size:
                enabled: true
                cpu: "900m"
                memory: "3984Mi"
                disk: 40Gi
            standard-8:
              size:
                enabled: true
                cpu: "1900m"
                memory: "8080Mi"
                disk: 80Gi
          sideCars:
            createBackup:
              requests:
                cpu: "100m"
                memory: "64Mi"
              limits:
                cpu: "400m"
                memory: "500Mi"
            clusterController:
              requests:
                cpu: "32m"
                memory: "128Mi"
              limits:
                cpu: "500m"
                memory: "512Mi"
            runDbops:
              requests:
                cpu: "100m"
                memory: "64Mi"
              limits:
                cpu: "500m"
                memory: "1Gi"
            setDbopsResult:
              requests:
                cpu: "100m"
                memory: "64Mi"
              limits:
                cpu: "250m"
                memory: "256Mi"
            envoy:
              requests:
                cpu: "32m"
                memory: "64Mi"
              limits:
                cpu: "500m"
                memory: "512Mi"
            pgbouncer:
              requests:
                cpu: "16m"
                memory: "4Mi"
              limits:
                cpu: "500m"
                memory: "128Mi"
            postgresUtil:
              requests:
                cpu: "10m"
                memory: "4Mi"
              limits:
                cpu: "20m"
                memory: "20Mi"
            prometheusPostgresExporter:
              requests:
                cpu: "10m"
                memory: "16Mi"
              limits:
                cpu: "150m"
                memory: "256Mi"
          initContainers:
            pgbouncerAuthFile:
              requests:
                cpu: "100m"
                memory: "100Mi"
              limits:
                cpu: "300m"
                memory: "500Mi"
            relocateBinaries:
              requests:
                cpu: "100m"
                memory: "100Mi"
              limits:
                cpu: "300m"
                memory: "500Mi"
            setupScripts:
              requests:
                cpu: "100m"
                memory: "500Mi"
              limits:
                cpu: "300m"
                memory: "500Mi"
            setupArbitraryUser:
              requests:
                cpu: "100m"
                memory: "500Mi"
              limits:
                cpu: "300m"
                memory: "500Mi"
            clusterReconciliationCycle:
              requests:
                cpu: "100m"
                memory: "100Mi"
              limits:
                cpu: "300m"
                memory: "200Mi"
            setDbopsRunning:
              requests:
                cpu: "250m"
                memory: "256Mi"
              limits:
                cpu: "250m"
                memory: "256Mi"
            setMajorVersionUpgrade:
              requests:
                cpu: "250m"
                memory: "256Mi"
              limits:
                cpu: "500m"
                memory: "512Mi"
            setupFilesystem:
              requests:
                cpu: "100m"
                memory: "100Mi"
              limits:
                cpu: "500m"
                memory: "512Mi"
        redis:
          enabled: true
          billing: true
          additionalInputs:
            kubectl_image: ${appcat:images:kubectl:registry}/${appcat:images:kubectl:image}:${appcat:images:kubectl:tag}
          serviceName: VSHNRedis
          compFunctionsOnly: true
          connectionSecretKeys:
            - ca.crt
            - tls.crt
            - tls.key
            - REDIS_HOST
            - REDIS_PORT
            - REDIS_USERNAME
            - REDIS_PASSWORD
            - REDIS_URL
            - SENTINEL_HOSTS
          mode: standalone
          offered: true
          restoreSA: redisrestoreserviceaccount
          restoreRoleRules: ${appcat:defaultRestoreRoleRules}
          openshiftTemplate:
            serviceName: redisbyvshn
            description: "The open source, in-memory data store used by millions of developers as a database, cache, streaming engine, and message broker."
            message: 'Your Redis by VSHN instance is being provisioned, please see \${SECRET_NAME} for access.'
            url: https://vs.hn/vshn-redis
            tags: "database,nosql"
            icon: "icon-redis"
            defaultVersion: "7.2"
          enableNetworkPolicy: true
          secretNamespace: ${appcat:services:vshn:secretNamespace}
          helmChartVersion: ${appcat:charts:redis:version}
          imageRegistry: ${appcat:images:redis:registry}
          imageRepositoryPrefix: ${appcat:images:redis:repositoryPrefix}
          maintenanceURL: "${appcat:images:redis:maintenanceURL}"
          grpcEndpoint: ${appcat:grpcEndpoint}
          proxyFunction: ${appcat:proxyFunction}
          defaultPlan: standard-1
          sla: 99.25
          plans:
            standard-512m:
              size:
                enabled: true
                cpu: "125m"
                memory: "512Mi"
                disk: 16Gi
            standard-1:
              size:
                enabled: true
                cpu: "250m"
                memory: "1Gi"
                disk: 16Gi
            standard-2:
              size:
                enabled: true
                cpu: "500m"
                memory: "2Gi"
                disk: 16Gi
            standard-4:
              size:
                enabled: true
                cpu: "1"
                memory: "4Gi"
                disk: 16Gi
            standard-8:
              size:
                enabled: true
                cpu: "2"
                memory: "8Gi"
                disk: 16Gi
          sideCars:
            sentinel:
              requests:
                cpu: "10m"
                memory: "50Mi"
              limits:
                cpu: "100m"
                memory: "100Mi"
            metrics:
              requests:
                cpu: "100m"
                memory: "128Mi"
              limits:
                cpu: "150m"
                memory: "192Mi"
            kubectl:
              requests:
                cpu: "10m"
                memory: "50Mi"
              limits:
                cpu: "100m"
                memory: "100Mi"
        minio:
          enabled: false
          billing: true
          serviceName: VSHNMinio
          compFunctionsOnly: true
          connectionSecretKeys:
            - MINIO_URL
            - AWS_SECRET_ACCESS_KEY
            - AWS_ACCESS_KEY_ID
          mode: distributed
          offered: false
          enableNetworkPolicy: true
          secretNamespace: ${appcat:services:vshn:secretNamespace}
          helmChartVersion: ${appcat:charts:minio:version}
          grpcEndpoint: ${appcat:grpcEndpoint}
          proxyFunction: ${appcat:proxyFunction}
          defaultPlan: standard-1
          imageRegistry: ""
          maintenanceURL: "${appcat:images:minio:maintenanceURL}"
          sla: 99.25
          plans:
            standard-1:
              size:
                enabled: true
                cpu: "1"
                memory: "1Gi"
                disk: 50Gi
          additionalInputs:
            providerEnabled: ${appcat:providers:minio:enabled}
            minioChartRepository: ${appcat:charts:minio:source} # for backwards compatibility for now
            minioChartVersion: ${appcat:charts:minio:version} # for backwards compatibility for now
          instances: []
        mariadb:
          enabled: false
          billing: true
          serviceName: VSHNMariaDB
          compFunctionsOnly: true
          connectionSecretKeys:
            - ca.crt
            - MARIADB_HOST
            - MARIADB_PORT
            - MARIADB_USERNAME
            - MARIADB_PASSWORD
            - MARIADB_URL
            - LOADBALANCER_IP
          mode: standalone
          offered: true
          restoreSA: mariadbrestoreserviceaccount
          restoreRoleRules: ${appcat:defaultRestoreRoleRules}
          additionalInputs:
            proxysqlImage: ${appcat:images:proxysql:registry}/${appcat:images:proxysql:image}:${appcat:images:proxysql:version}
            proxysqlCPULimit: 500m
            proxysqlCPURequests: 50m
            proxysqlMemoryLimit: 256Mi
            proxysqlMemoryRequests: 64Mi
          openshiftTemplate:
            serviceName: mariadbbyvshn
            description: "The open source relational database management system (DBMS) that is a compatible drop-in replacement for the widely used MySQL database technology"
            message: 'Your MariaDB by VSHN instance is being provisioned, please see \${SECRET_NAME} for access.'
            url: https://vs.hn/vshn-mariadb
            tags: "database,sql,mariadb"
            icon: "icon-mariadb"
            defaultVersion: "11.5"
          enableNetworkPolicy: false
          secretNamespace: ${appcat:services:vshn:secretNamespace}
          helmChartVersion: ${appcat:charts:mariadb:version}
          imageRegistry: ${appcat:images:mariadb:registry}
          imageRepositoryPrefix: ${appcat:images:mariadb:repositoryPrefix}
          maintenanceURL: "${appcat:images:mariadb:maintenanceURL}"
          grpcEndpoint: ${appcat:grpcEndpoint}
          proxyFunction: ${appcat:proxyFunction}
          defaultPlan: standard-1
          sla: 99.25
          plans:
            standard-512m:
              size:
                enabled: true
                cpu: "125m"
                memory: "512Mi"
                disk: 16Gi
            standard-1:
              size:
                enabled: true
                cpu: "250m"
                memory: "1Gi"
                disk: 16Gi
            standard-2:
              size:
                enabled: true
                cpu: "500m"
                memory: "2Gi"
                disk: 16Gi
            standard-4:
              size:
                enabled: true
                cpu: "1"
                memory: "4Gi"
                disk: 16Gi
            standard-8:
              size:
                enabled: true
                cpu: "2"
                memory: "8Gi"
                disk: 16Gi
        keycloak:
          enabled: false
          billing: true
          serviceName: VSHNKeycloak
          compFunctionsOnly: true
          connectionSecretKeys:
            - KEYCLOAK_HOST
            - KEYCLOAK_PASSWORD
            - KEYCLOAK_URL
            - KEYCLOAK_USERNAME
            - ca.crt
          mode: standalone
          offered: true
          restoreSA: keycloakserviceaccount
          restoreRoleRules: ${appcat:defaultRestoreRoleRules}
          additionalInputs:
            busybox_image: ${appcat:images:busybox:registry}/${appcat:images:busybox:image}
            kubectl_image: ${appcat:images:kubectl:registry}/${appcat:images:kubectl:image}:${appcat:images:kubectl:tag}
            registry_username: "?{vaultkv:__shared__/__shared__/appcat/inventage_registry_username}"
            registry_password: "?{vaultkv:__shared__/__shared__/appcat/inventage_registry_password}"
            ingress_annotations: |
              cert-manager.io/cluster-issuer: letsencrypt-production
            defaultPGComposition: vshnpostgres.vshn.appcat.vshn.io
          openshiftTemplate:
            serviceName: keycloakbyvshn
            description: "Keycloak is an open source identity and access management solution."
            message: 'Your Keycloak by VSHN instance is being provisioned, please see \${SECRET_NAME} for access.'
            url: https://vs.hn/vshn-keycloak
            tags: "idp,keycloak"
            icon: "icon-keycloak"
            defaultVersion: "22"
          enableNetworkPolicy: false
          secretNamespace: ${appcat:services:vshn:secretNamespace}
          helmChartVersion: ${appcat:charts:keycloak:version}
          imageRegistry: ${appcat:images:keycloak:registry}/${appcat:images:keycloak:image}
          maintenanceURL: "${appcat:images:keycloak:maintenanceURL}"
          grpcEndpoint: ${appcat:grpcEndpoint}
          proxyFunction: ${appcat:proxyFunction}
          defaultPlan: standard-2
          sla: 99.25
          plans:
            standard-2:
              size:
                enabled: true
                cpu: "500m"
                memory: "2Gi"
                disk: 16Gi
            standard-4:
              size:
                enabled: true
                cpu: "1"
                memory: "4Gi"
                disk: 16Gi
            standard-8:
              size:
                enabled: true
                cpu: "2"
                memory: "8Gi"
                disk: 16Gi
        nextcloud:
          enabled: false
          billing: true
          serviceName: VSHNNextcloud
          compFunctionsOnly: true
          connectionSecretKeys:
            - NEXTCLOUD_HOST
            - NEXTCLOUD_PASSWORD
            - NEXTCLOUD_URL
            - NEXTCLOUD_USERNAME
            - COLLABORA_HOST
            - ca.crt
          mode: standalone
          offered: true
          restoreSA: nextcloudserviceaccount
          restoreRoleRules: ${appcat:defaultRestoreRoleRules}
          additionalInputs:
            collabora_image: ${appcat:images:collabora:registry}/${appcat:images:collabora:image}
            collabora_image_tag: ${appcat:images:collabora:tag}
            collaboraCPULimit: "1"
            collaboraCPURequests: 250m
            collaboraMemoryLimit: 1Gi
            collaboraMemoryRequests: 256Mi
            ingress_annotations: |
              cert-manager.io/cluster-issuer: letsencrypt-production
            nextcloud_image_repository: ${appcat:images:nextcloud:image}
            busybox_image: ${appcat:images:busybox:registry}/${appcat:images:busybox:image}
            kubectl_image: ${appcat:images:kubectl:registry}/${appcat:images:kubectl:image}:${appcat:images:kubectl:tag}
            defaultPGComposition: vshnpostgres.vshn.appcat.vshn.io
          openshiftTemplate:
            serviceName: nextcloudbyvshn
            description: "Nextcloud is an open source suite of client-server software for creating and using file hosting services."
            message: 'Your Nextcloud by VSHN instance is being provisioned, please see \${SECRET_NAME} for access.'
            url: https://vs.hn/vshn-nextcloud
            tags: "idp,nextcloud"
            icon: "icon-nextcloud"
            defaultVersion: "22"
          enableNetworkPolicy: false
          secretNamespace: ${appcat:services:vshn:secretNamespace}
          helmChartVersion: ${appcat:charts:nextcloud:version}
          imageRegistry: ${appcat:images:nextcloud:registry}
          maintenanceURL: "${appcat:images:nextcloud:maintenanceURL}"
          grpcEndpoint: ${appcat:grpcEndpoint}
          proxyFunction: ${appcat:proxyFunction}
          defaultPlan: standard-2
          sla: 99.25
          plans:
            standard-2:
              size:
                enabled: true
                cpu: "500m"
                memory: "2Gi"
                disk: 16Gi
            standard-4:
              size:
                enabled: true
                cpu: "1"
                memory: "4Gi"
                disk: 16Gi
            standard-8:
              size:
                enabled: true
                cpu: "2"
                memory: "8Gi"
                disk: 16Gi
        codey:
          enabled: true
          billing: false
          serviceName: Codey
          connectionSecretKeys:
            - CODEY_HOST
            - CODEY_PASSWORD
            - CODEY_URL
            - CODEY_USERNAME
            - ca.crt
          mode: standalone
          offered: true
          restoreSA: codeyserviceaccount
          restoreRoleRules: ${appcat:defaultRestoreRoleRules}
          additionalInputs:
            smtpUsername: "?{vaultkv:__shared__/__shared__/appcat/codey_smtp_username}"
            smtpPassword: "?{vaultkv:__shared__/__shared__/appcat/codey_stmp_password}"
          openshiftTemplate:
            serviceName: codey
            description: "Codey is a secure, fast and reliable code collaboration platform made and hosted in Europe, with 100% free software."
            message: 'Your Codey instance is being provisioned, please see \${SECRET_NAME} for access.'
            url: https://www.codey.ch/
            tags: "codehosting,codey,fogejo"
            icon: "icon-codey"
            defaultVersion: "22"
          enableNetworkPolicy: false
          secretNamespace: ${appcat:services:vshn:secretNamespace}
          imageRegistry: ""
          defaultPlan: mini
          sla: 99.25
          plans:
            mini:
              size:
                enabled: true
                cpu: "500m"
                memory: "2Gi"
                disk: 10Gi
            small:
              size:
                enabled: true
                cpu: "1"
                memory: "4Gi"
                disk: 50Gi
      # Config for exoscale composites
      exoscale:
        enabled: false
        secretNamespace: ${appcat:crossplane:namespace}
        providerSecretNamespace: ${appcat:providers:exoscale:connectionSecretNamespace}
        mysql:
          enabled: true
          secretNamespace: ${appcat:services:exoscale:secretNamespace}
          providerSecretNamespace: ${appcat:services:exoscale:providerSecretNamespace}
        postgres:
          enabled: true
          secretNamespace: ${appcat:services:exoscale:secretNamespace}
          providerSecretNamespace: ${appcat:services:exoscale:providerSecretNamespace}
        redis:
          enabled: true
          secretNamespace: ${appcat:services:exoscale:secretNamespace}
          providerSecretNamespace: ${appcat:services:exoscale:providerSecretNamespace}
        kafka:
          enabled: true
          secretNamespace: ${appcat:services:exoscale:secretNamespace}
          providerSecretNamespace: ${appcat:services:exoscale:providerSecretNamespace}
        opensearch:
          enabled: true
          secretNamespace: ${appcat:services:exoscale:secretNamespace}
          providerSecretNamespace: ${appcat:services:exoscale:providerSecretNamespace}

      # Config for "generic" vendor agnositc composites that have more than one matching composition
      generic:
        objectstorage:
          enabled: false

          defaultComposition: ${facts:cloud}
          # Compositions that provide a composition for the composite with it's configuration
          compositions:
            exoscale:
              enabled: true
              secretNamespace: ${appcat:services:generic:objectstorage:secretNamespace}
              providerSecretNamespace: ${appcat:providers:exoscale:connectionSecretNamespace}
              grpcEndpoint: ${appcat:grpcEndpoint}
              proxyFunction: ${appcat:proxyFunction}
            cloudscale:
              enabled: true
              secretNamespace: ${appcat:services:generic:objectstorage:secretNamespace}
              providerSecretNamespace: ${appcat:providers:cloudscale:connectionSecretNamespace}
              grpcEndpoint: ${appcat:grpcEndpoint}
              proxyFunction: ${appcat:proxyFunction}
            minio:
              enabled: false
              secretNamespace: ${appcat:services:generic:objectstorage:secretNamespace}
              providerSecretNamespace: ${appcat:providers:cloudscale:connectionSecretNamespace}
              grpcEndpoint: ${appcat:grpcEndpoint}
              proxyFunction: ${appcat:proxyFunction}
              providerConfig: minio

          # Generic config for objecstorage composite
          secretNamespace: ${appcat:crossplane:namespace}
