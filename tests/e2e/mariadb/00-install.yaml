apiVersion: vshn.appcat.vshn.io/v1
kind: VSHNMariaDB
metadata:
  labels:
    appcat.vshn.io/provider-config: kind
  name: mariadb-e2e-to-delete
spec:
  parameters:
    instances: 3
    security:
      deletionProtection: false
    size:
      plan: standard-2
  writeConnectionSecretToRef:
    name: mariadb-creds
