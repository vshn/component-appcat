apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  labels:
    syn: 'true'
    syn_component: appcat
    syn_team: schedar
  name: appcat-sliexporter-health
  namespace: syn-appcat-slos
spec:
  groups:
    - name: appcat-sliexporter-health
      rules:
        - alert: AppCatSLIExporterDown
          annotations:
            description: The AppCat SLI exporter is not reachable by Prometheus. SLO
              measurements are not being collected, which may silently mask SLA violations.
            summary: AppCat SLI exporter is down
            title: AppCat SLI exporter is down
          expr: absent(up{job="appcat-sliexporter-controller-manager-metrics-service"})
            or up{job="appcat-sliexporter-controller-manager-metrics-service"} ==
            0
          for: 5m
          labels:
            OnCall: 'false'
            runbook: https://kb.vshn.ch/app-catalog/how-tos/appcat/AppCatSLIExporterDown.html
            service: AppCatSLIExporter
            severity: critical
            syn: 'true'
            syn_component: appcat
            syn_team: schedar
        - alert: AppCatSLIExporterNoMetrics
          annotations:
            description: The AppCat SLI exporter is running but no probe metrics are
              being reported. SLO measurements may be missing, which could silently
              mask SLA violations.
            summary: AppCat SLI exporter reports no probe metrics
            title: AppCat SLI exporter reports no probe metrics
          expr: absent(appcat_probes_seconds_count)
          for: 10m
          labels:
            OnCall: 'false'
            runbook: https://kb.vshn.ch/app-catalog/how-tos/appcat/AppCatSLIExporterDown.html
            service: AppCatSLIExporter
            severity: warning
            syn: 'true'
            syn_component: appcat
            syn_team: schedar
